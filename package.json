{
  "name": "@sengac/fspec",
  "version": "0.10.0",
  "description": "fspec: The Spec-Driven, Multi-Agent Coding Factory",
  "scripts": {
    "build:rust:fspec": "(cd rust && cargo build -p codelet-fspec --release) && mkdir -p dist && cp rust/target/release/fspec dist/fspec && chmod +x dist/fspec",
    "bridge:telegram": "npx tsx bridge/telegram-endpoint.ts",
    "bridge:telegram:bg": "nohup npx tsx bridge/telegram-endpoint.ts > bridge/telegram.log 2>&1 & echo $! > bridge/telegram.pid && echo 'Telegram bridge started in background (PID saved to bridge/telegram.pid)'",
    "bridge:telegram:stop": "kill $(cat bridge/telegram.pid) 2>/dev/null && rm bridge/telegram.pid && echo 'Telegram bridge stopped' || echo 'No running bridge found'",
    "bridge:relay": "npx tsx bridge/relay-endpoint.ts",
    "bridge:relay:bg": "nohup npx tsx bridge/relay-endpoint.ts > bridge/relay.log 2>&1 & echo $! > bridge/relay.pid && echo 'Relay bridge started in background (PID saved to bridge/relay.pid)'",
    "bridge:relay:stop": "kill $(cat bridge/relay.pid) 2>/dev/null && rm bridge/relay.pid && echo 'Relay bridge stopped' || echo 'No running bridge found'",
    "bridge:server": "npx tsx bridge/relay-server-cli.ts",
    "bridge:server:bg": "nohup npx tsx bridge/relay-server-cli.ts > bridge/server.log 2>&1 & echo $! > bridge/server.pid && echo 'Relay server started in background (PID saved to bridge/server.pid)'",
    "bridge:server:stop": "kill $(cat bridge/server.pid) 2>/dev/null && rm bridge/server.pid && echo 'Relay server stopped' || echo 'No running bridge server found'"
  },
  "keywords": [
    "spec-driven-development",
    "spec-driven",
    "acdd",
    "acceptance-criteria-driven-development",
    "acceptance-criteria",
    "kanban",
    "workflow",
    "bdd",
    "gherkin",
    "feature-files",
    "ai-agents",
    "agentic-ai",
    "llm",
    "claude-code",
    "project-management",
    "work-units",
    "example-mapping",
    "specification",
    "test-driven-development",
    "tdd",
    "coverage-tracking",
    "ai-workflow"
  ],
  "author": "rquast@sengac.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/sengac/fspec.git"
  },
  "homepage": "https://github.com/sengac/fspec#readme",
  "bugs": {
    "url": "https://github.com/sengac/fspec/issues"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "node-telegram-bot-api": "^0.66.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {}
}
