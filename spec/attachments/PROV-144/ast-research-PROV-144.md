# PROV-144 AST Research: Per-profile Max Images + Read tool image budget enforcement

**Method:** GraphSearch AST index (incremental re-index of `rust/`, 2,749 files / 24,533
functions / 2,308 types) + Grep cross-checks for call sites. Date: 2026-09-04.

## 1. Wire struct — `ProfileDefinition` (AST: Type, struct_kind, public)

- **Slug:** `rpc-types-src-lib-rs::ProfileDefinition`
- **Location:** `rust/rpc-types/src/lib.rs:476-505`
- **Fields (current):** `base_url: String`, `api_key: String`, `context_window: Option<u32>`,
  `max_output_tokens: Option<u32>`, `compaction_threshold_type: Option<String>`,
  `compaction_threshold_value: Option<u32>`, `streaming: Option<bool>` (PROV-139),
  `auto_continue: Option<u32>` (PROV-142), `preserve_thinking: Option<bool>` (PROV-143).
- **Decorators:** `#[cfg_attr(feature = "napi", napi_derive::napi(object))]`,
  `#[derive(Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize)]`.
- **Existing predicates** (impl block, lib.rs:507-531): `streaming_enabled()`,
  `auto_continue_enabled()`, `preserve_thinking_enabled()` — the canonical-predicate
  pattern to mirror for `max_images_limit()`.
- **PROV-144 change:** append `pub max_images: Option<u32>` after `preserve_thinking`;
  add `max_images_limit(&self) -> u32` (absent ⇒ 4, `Some(n)` ⇒ n) to the impl block.

## 2. On-disk struct — `ProfileDef` (AST: Type, struct_kind, public)

- **Slug:** `sessions-src-profile_persistence-rs::ProfileDef`
- **Location:** `rust/sessions/src/profile_persistence.rs:35-56`
- **Derives:** `Debug, Clone, PartialEq` — **NO `Default`**, so every full-literal
  construction site breaks when a field is added.
- **Write path:** `merge_profile()` (line 169) — established
  `set_or_remove(profile, "<camelKey>", def.field.map(Value::from))` pattern.
  PROV-144 adds `set_or_remove(profile, "maxImages", def.max_images.map(Value::from))`.

## 3. NAPI TS projection — `ProfileDefinition` interface

- **Slug:** `napi-index-d-ts::ProfileDefinition`
- **Location:** `rust/napi/index.d.ts:3457-3482`
- Auto-generated by `napi build` (no pnpm in this environment → hand-edit mirroring the
  `autoContinue?: number;` doc-comment style for `maxImages?: number;`).

## 4. Tool-layer registry — `set_session_model_vision` (AST: Function, public)

- **Slug:** `tools-src-model_capabilities-rs::set_session_model_vision`
- **Location:** `rust/tools/src/model_capabilities.rs:25-31`
- **Signature:** `(session_id: Uuid, supports_vision: bool)`
- **Siblings in module:** `session_has_capabilities`, `session_supports_vision`,
  `clear_session_model_vision`, `clear_all_model_capabilities` — all over a single
  `once_cell::sync::Lazy<RwLock<HashMap<Uuid, bool>>>` with poison-graceful guards.
- **PROV-144 change (option 1, recommended in research doc):** sibling static
  `SESSION_MODEL_MAX_IMAGES: Lazy<RwLock<HashMap<Uuid, u32>>>` +
  `set_session_model_max_images` / `session_model_max_images` /
  `clear_session_model_max_images`; `clear_all_model_capabilities` must clear BOTH.

### Set-sites (callers of `set_session_model_vision`, Grep-verified — 5 call expressions):

| # | File | Line | Function |
|---|------|------|----------|
| 1 | `rust/sessions/src/session_creation_helper.rs` | 171 | `create_background_session_inner` (shared create) |
| 2 | `rust/sessions/src/session_manager.rs` | 1033 | `create_isolated_session_with_id` |
| 3 | `rust/sessions/src/handle_impl.rs` | 1373 | mid-session `set_model` |
| 4 | `rust/napi/src/session_bindings.rs` | 2060 | `session_set_model` |
| 5 | `rust/napi/src/session_bindings.rs` | 2241 | `session_set_model_profile` |

All 5 must additionally call `set_session_model_max_images(uuid, budget)` where the
budget is resolved via the new `resolve_profile_max_images` / default-4 predicate.

### Clear-sites:

- `rust/sessions/src/session_manager.rs:1270` — `destroy_session`
  (`clear_session_model_vision(uuid)`); must also clear the new max-images entry.

## 5. Resolution — `model_resolution.rs`

- `resolve_model_vision(pm: &ProviderManager) -> bool` (line 23) — profile branch
  (28-41) parses `openai:<profile>/<model>`, reads `customModels[].hasVision` from
  `load_local_server_profiles()`. PROV-144 adds sibling
  `resolve_profile_max_images(pm) -> Option<u32>` reusing the same
  `load_local_server_profiles()` lookup; the profile object now carries `max_images`
  from the `LocalServerProfile` deser change (profile_sections.rs:83, lenient
  `de_opt_u32_lenient` at line 177).
- `apply_profile_env_vars` (line 224) is the env bridge — **must NOT gain maxImages**
  (tool-layer concern only).

## 6. Read tool — `read.rs` (456 lines, over the 300-line ceiling)

- `ReadTool::call` (line 272) is the single enforcement point (FileToolFacadeWrapper
  delegates to it — facade/wrapper.rs:346/363 constructs `ReadTool::new(session_id)`).
- Image-returning branches: `FileType::Image(media_type)` (324-327, 1 image),
  PDF `ExemptFileType::Pdf` → "images" mode (397-410), visual/default mode (411-424,
  N pages capped by `max_pdf_pages()` = 20).
- Error type: `ToolError::Validation { tool, message }` (tools/src/error.rs:30) —
  no new variant needed.
- **PROV-144 change:** extract budget enforcement into new module
  `rust/tools/src/read_image_budget.rs` (module decl in tools/src/lib.rs, alphabetical
  between `read` and `request_user_input`): budget resolution (registry → default 4),
  the no-vision failure messages, and the over-budget failure builder. `read.rs` wires
  it into the Image branch + PDF visual/images branches, plus the
  `session_has_capabilities`-style PDF default-mode fallback when budget == 0.

## 7. TUI form (9th field appended after Preserve Thinking)

- `profile_form.rs:33` — `PROFILE_FORM_FIELDS: [&str; 8]` → `[&str; 9]` ("Max Images").
- `ProfileForm` (line 47) gains `pub max_images: String` (raw text, parse-on-build).
- `new_create()` (75): seed `"4"`. `from_definition()` (98): prefill
  `def.max_images.map(to_string).unwrap_or_else(|| "4")`.
- `focused_text_mut()` (122): `8 => &mut self.max_images`; `field_value()` (181):
  `8 => &self.max_images`.
- `build_definition()` (201): `parse_max_images(&self.max_images)?` (empty ⇒ None,
  "0" ⇒ Some(0), "n" ⇒ Some(n), non-numeric ⇒ Err hint) — the `Err(hint)` rejection
  path already exists in `handle_form_key` (286-290) from PROV-142.
- `profile_form_parse.rs` (64 lines): add `parse_max_images`.
- `profile_form_render.rs:54` `placeholder_for`: `8 => "4 (default), 0 = no vision"`.
- `profile_form_streaming.rs`: boolean-toggle guard special-cases indices 5 and 7 only
  — index 8 routes to the text-editing branch automatically. No routing change.
- `profiles_config.rs:130` `profile_definition_from_value`: read `maxImages`
  (as_u64 → u32, mirroring `autoContinue` at line 158).

## 8. Full-literal construction sites that adding a field breaks

### `ProfileDef` (no Default derive — MUST update):

1. `rust/sessions/src/conversions.rs:178` (`profile_def_from_wire`)
2. `rust/sessions/src/profile_persistence_tests.rs:14` (`basic`)
3. `rust/sessions/src/profile_persistence_tests.rs:82`
4. `rust/sessions/tests/prov142_auto_continue_persistence.rs:25` (`def_auto_continue`)
5. `rust/sessions/tests/prov139_streaming_persistence.rs:25` (`def_streaming`)
6. `rust/sessions/tests/prov143_preserve_thinking_persistence.rs:26` (`def_preserve`)

### `ProfileDefinition` (has Default — full literals update optionally,
`..ProfileDefinition::default()` sites unaffected):

- Production: `rust/fspec-tui/src/views/provider_settings/profiles_config.rs:165`,
  `rust/fspec-tui/src/views/provider_settings/profile_form.rs:211` (MUST — new field).
- Test literals: rpc-types tests (139, 142), sessions tests (139, 142, 143,
  profile_rpc_surface.rs:129, common/profile_rpc_helpers.rs:75), fspec-tui tests
  (139, 142, 143, prov102, prov109, prov111, prov116, prov110) — all compile without
  change (Default) but PROV-143's `prov143_preserve_thinking_form.rs:81-85` asserts
  `pt_idx == PROFILE_FORM_FIELDS.len() - 1` ("Preserve Thinking must be the last form
  field") and **will fail** once the 9th field exists → update that assertion to
  `pt_idx == auto_idx + 1` only (the positional invariant), moving the
  last-field invariant to the new PROV-144 test.

## 9. Test fixtures available for reuse

- `rust/tools/tests/pdf_vision_fallback_test.rs` — `create_test_pdf_with_pages(n)`
  (lopdf-built text PDFs) + `#[serial]` registry tests + `ReadTool::call` invocation
  pattern. Reuse for PROV-144 read-tool budget tests.
- `rust/tools/tests/read_multimodal_test.rs` — 69-byte minimal 1×1 red PNG;
  `make_png_header(w,h)` in image_dimensions_test.rs.
- `rust/sessions/tests/bug168_model_vision_resolution.rs` — `write_profile_config`,
  `tmp_dir_with_env("FSPEC_USER_DIR")`, `EnvGuard` fixtures for resolution tests.
- `rust/sessions/tests/prov142_auto_continue_persistence.rs` — `save_profile_at`
  round-trip pattern for the persistence test.
- `rust/rpc-types/tests/prov142_auto_continue_flag.rs` — wire predicate + JSON
  round-trip pattern.
- `rust/fspec-tui/tests/prov142_auto_continue_form.rs` — key-driven form test pattern
  (create_view, type_chars, build_definition, handle_key(Enter) rejection check).

## 10. Test-file plan (RED phase)

| Crate | New test file | Scenarios covered (feature: per-profile-max-images-limit-read-tool-image-budget-enforcement.feature) |
|---|---|---|
| codelet-rpc-types | `tests/prov144_max_images_wire.rs` | "A missing maxImages key resolves to the default 4" (predicate half), wire JSON round-trip |
| codelet-sessions | `tests/prov144_max_images_persistence.rs` | "The maxImages value round-trips through wire and disk", "A missing maxImages key resolves to the default 4" (disk half) |
| codelet-sessions | `tests/prov144_max_images_resolution.rs` | budget follows profile at creation + mid-session switch (registry-level: `resolve_profile_max_images`) |
| codelet-tools | `tests/prov144_read_image_budget.rs` | no-vision image-file fail, no-vision PDF→text, over-budget PDF visual fail + retry limit, within-budget PDF+PNG succeed, images-mode over-budget fail, non-profile default-4 (JPEG ok / 5-page PDF fail), registry set/get/clear |
| codelet-fspec-tui | `tests/prov144_max_images_form.rs` | prefill default 4, prefill stored value, empty ⇒ None, "0" ⇒ Some(0), non-numeric rejects save with hint, field appears after Preserve Thinking (9th/last) |
