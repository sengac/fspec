{
  "name": "fspec-browser-agent",
  "version": "0.1.0",
  "description": "fspec Browser Agent - Chrome extension for AI agent browser control and WebMCP tool discovery",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build -c vite.popup.config.ts && vite build -c vite.content.config.ts",
    "dev": "vite build --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "@vitest/browser": "^4.0.18",
    "jsdom": "^28.1.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.0.18"
  }
}
